---
"@appspine/common": minor
---

Add the `@appspine/common` package: `GlobalExceptionFilter`, `ZodValidationPipe`, pagination helpers, `PermissionPolicy`/`AuditAction` enums, `PrismaModule`/`PrismaService` (resolves `@prisma/client` from the consuming app), and `LoggingModule`. Ported from `auranest/packages/@auranest/backend-core` per `dev_docs/003-shared-package-reuse-plan.md`.
