import { SystemAdminGuard } from '@appspine/plugin-host-nest';

/** RBAC's name for the host's `SystemAdminGuard`. Alias, not a copy (Gate G1 review S9). */
export { SystemAdminGuard as RbacAdminGuard };
