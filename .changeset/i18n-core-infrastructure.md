---
"@appspine/frontend-shell": minor
---

Add core i18n infrastructure, supporting locale types, a type-safe messages helper `buildAllMessages`, the context-providing `I18nProvider`, and client hooks `useLocale`/`useTranslations` for shared translation retrieval.
