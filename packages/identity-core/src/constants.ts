/**
 * Re-exported from `@appspine/plugin-api`, which owns the names (Gate G1 review S9). Kept as a
 * package-local module because both names are part of this package's published surface — and,
 * for `@appspine/identity-core`, of the `@appspine/auth` facade's.
 */
export { SYSTEM_ADMIN_ROLE, SYSTEM_USER_ROLE } from '@appspine/plugin-api';
